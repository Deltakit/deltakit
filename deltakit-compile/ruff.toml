# Linting configuration for deltakit_compiler.
#
# General and format configurations are repository-wide for the moment.
extend = "../ruff.toml"
include = ["src/deltakit_compiler/**/*.py", "tests/**/*.py"]